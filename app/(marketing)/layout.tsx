import type { ReactNode } from "react";

/** Route shell reserved for the public marketing experience. */
export default function MarketingLayout({ children }: Readonly<{ children: ReactNode }>) {
  return children;
}
